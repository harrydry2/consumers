script.
  var host = "www.websitesformusicians.co";
  if ((host == window.location.host) && (window.location.protocol != "https:")) {
    window.location.protocol = "https";
  }

include mixins/logo

doctype html
html(lang='en')
  head
    meta(charset='UTF-8')
    meta(name='viewport', content='width=device-width, initial-scale=1.0')
    meta(http-equiv='X-UA-Compatible', content='ie=edge')
    link(rel='stylesheet', href='/dist/bundle.css')
    title Sites For Musicians
    script(src="https://js.stripe.com/v3/")
  body
    .p1Container
      .p1Background
      +logo
      .mainTextContainer
        h1 Simple, All Inclusive Pricing
        h2 Order now, and you’re site will be ready in 24 hours

    form(action="/charge" method="POST" id="payment-form")
      input(type="text" name="stripeEmail" class="formInput" placeholder="Email Address")
      input(type="text" name="stripeName" class="formInput" placeholder="Name")
      #card-element
      #card-errors(role="alert")
      input(type="text" name="stripeCoupon" class="formInput" placeholder="Enter a Coupon Code")    
      button Pay        
      block scripts
        script(src="/dist/bundle.js")